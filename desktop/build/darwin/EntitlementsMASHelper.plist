<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Entitlements for the meron-core sidecar inside the App Store bundle.

  com.apple.security.inherit means the child adopts the parent's sandbox exactly
  as it stands at exec time, which is how it gets network access for IMAP/SMTP
  and reach into the app's keychain items. It is only valid on its own: pairing
  inherit with any other entitlement makes the exec fail, so this file must stay
  as it is even as EntitlementsMAS.plist grows.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.app-sandbox</key>
    <true/>
    <key>com.apple.security.inherit</key>
    <true/>
</dict>
</plist>
